name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions: {}

jobs:
  html:
    name: html5 validate
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: '3.12'
      - run: pip install html5validator
      - run: |
          html5validator \
            --root . \
            --match "*.html" \
            --ignore "index.html.pre-strip-backup" "_tour.html" \
            --ignore-re "seed/" "dispatches/" "partials/" \
              "ARIA attribute .aria-pressed. is not allowed" \
              "Property .inset. doesn" \
              "Property .backdrop-filter. doesn" \
              "Property .print-color-adjust. doesn" \
              "Property .text-underline-offset. doesn" \
              "Attribute .media. not allowed on element .meta." \
              "Element .style. not allowed as child of element .div." \
              "linear-gradient" \
            --log INFO

  links:
    name: link check
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: lycheeverse/lychee-action@8646ba30535128ac92d33dfc9133794bfdd9b411  # v2
        with:
          args: >-
            --no-progress
            --base-url https://loci.garden
            --exclude "^#"
            --exclude "loci\.garden"
            --exclude "localhost"
            --exclude "127\.0\.0\.1"
            --accept 200,429
            './*.html'
            './releases/index.html'
            './zoku/index.html'
          fail: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
